# VoltFlow Frontend (Vite) — copy this file to ".env" and set the value.
# Read in code with: import.meta.env.VITE_SERVER_URL
# Base URL of the backend server (no trailing slash, no /api suffix).
VITE_SERVER_URL=http://localhost:5001
