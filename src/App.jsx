import AppRoutes from "./routes/AppRoutes.jsx";

// App is intentionally minimal — it only renders the router. The full route
// tree lives in routes/AppRoutes.jsx.
export default function App() {
  return <AppRoutes />;
}
